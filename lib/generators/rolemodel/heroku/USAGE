Description:
    Sets up our standard Heroku deployment configuration

Example:
    rails generate rolemodel:heroku

    This will create:
        bin/heroku_release
        app.json
        Procfile
